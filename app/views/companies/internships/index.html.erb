<%= render 'partial/employee_mypage_layout' do %>
<h2 class="mb-4">インターンシップ一覧</h2>

<% if @internships.empty? %>
<p>ご登録されているインターンシップはありません</p>
<% end %>

<ul class="list-group list-group-flush">
  <% @internships.each do |internship| %>
  <%= link_to internship.title, company_internship_path(id: internship.id), class: "list-group-item list-group-item-action" %>
  <% end %>
</ul>

<% end %>
