<%= render 'partial/student_mypage_layout' do %>
<h2 class="mb-4">インターンシップ一覧</h2>

<p>申し込み済み</p>
<% if @orders.empty? %>
<p>お申し込み済みのインターンシップはありません</p>
<% end %>
<ul class="list-group">
  <% @orders.each do |order| %>
  <li class="list-group-item list-group-item-action d-flex justify-content-between align-items-center">
    <%= link_to order.internship.title, internship_path(id: order.internship.id), class: "link-primary" %>
    <%= button_to "連絡したボタン",
      student_internship_order_contact_path(student_id: current_student.id, internship_order_id: order.id),
      class: 'btn btn-primary text-white'
    %>
  </li>
  <% end %>
</ul>

<p class="mt-5">お気に入り</p>
<% if @carts.empty? %>
<p>お気に入りのインターンシップはありません</p>
<% end %>
<ul class="list-group">
  <% @carts.each do |cart| %>
  <li class="list-group-item list-group-item-action d-flex justify-content-between align-items-center">
    <%= link_to cart.internship.title, internship_path(id: cart.internship.id), class: "link-primary" %>
    <div class="d-flex gap-3">
      <%= button_to "申し込む", student_internship_orders_path(internship_cart_id: cart.id), class: 'btn btn-warning btn-sm' %>
      <%= button_to "キャンセル", student_internship_cart_path(id: cart.id), method: :delete, class: 'btn btn-danger btn-sm' %>
    </div>
  </li>
  <% end %>
</ul>

<% end %>
